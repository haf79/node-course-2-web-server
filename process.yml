apps:
  - script   : ./server.js
    name     : 'api-app'
    instances: 0
    exec_mode: cluster
    watch  : true
    env    :
      NODE_ENV: development
    env_production:
      NODE_ENV: production
